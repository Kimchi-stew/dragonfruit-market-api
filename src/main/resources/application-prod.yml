spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}

server:
  port: 8282
  servlet:
    context-path: /api
  forward-headers-strategy: FRAMEWORK

springdoc:
  swagger-ui:
    enabled: true
